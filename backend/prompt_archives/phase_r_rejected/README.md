# Rejected Phase R narration planner

`narration_coverage_contract.planner-v1.json` is the byte-preserved rejected
preclassification prompt. It is historical evidence only, outside the active
`prompt_templates` directory. No production loader, feature flag or fallback may
load it. Historical Run/LLMLog snapshots remain unchanged.

Approved replacement: the same Shot Director declares source treatments;
`narration_coverage.py` performs deterministic validation only.
